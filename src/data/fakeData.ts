// Phase 1 假資料：用來驗證地形生成與機車手感，尚未接真實台股
// 刻意設計出各種地形：緩坡、陡升(漲停牆)、急跌(跳台)、連續抖動

// 一條「戲劇性」的手工序列，刻意放入漲停/跌停級的尖峰當跳台 kicker：
// 連續急升到尖頂 → 緊接急跌 → 車子從尖頂飛出去、有滯空可後空翻 → 落地跑道
export const SAMPLE_PRICES: number[] = [
  100, 100, 101, 102, 103, 104, // 0-5  起步跑道
  113, 132, 152, // 6-8  漲停級陡升 → 尖頂(跳台1)
  104, // 9    跌停級急跌 → 從尖頂飛出
  106, 107, 108, 108, 109, // 10-14 落地跑道
  126, 150, 172, // 15-17 更大的 kicker → 高尖頂(跳台2)
  108, // 18   斷崖式急跌
  110, 111, 112, 112, 113, // 19-23 跑道
  130, 155, 178, 188, // 24-27 連續漲停堆高 → 最高跳台3
  118, // 28   巨幅跌停
  116, 117, 118, 118, 119, // 29-33 跑道
  138, 160, 180, // 34-36 kicker4
  112, // 37   急跌
  114, 115, 116, 116, 117, 117, // 38-43 收尾跑道
];

// 隨機漫步產生器：之後可用來大量測試不同波動度的賽道
export function randomWalk(
  points = 60,
  start = 100,
  volatility = 3,
  seed = Date.now(),
): number[] {
  // 簡單可重現的偽隨機 (mulberry32)
  let s = seed >>> 0;
  const rng = () => {
    s |= 0;
    s = (s + 0x6d2b79f5) | 0;
    let t = Math.imul(s ^ (s >>> 15), 1 | s);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
  const out: number[] = [start];
  for (let i = 1; i < points; i++) {
    const step = (rng() - 0.5) * 2 * volatility;
    out.push(Math.max(1, out[i - 1] + step));
  }
  return out;
}
