# Cloudflare Pages 安全 headers（SECURITY_REVIEW 2026-07-04 第二輪）
# CSP 2026-07-04 晚轉正式執法：先以 Report-Only 部署，使用者真機走完
# 「登入 Google → 玩每日挑戰 → 分享成績」全流程確認 console 零違規後改名轉正。
# 之後若加新外部資源出現被擋（console 會有紅字 Refused to ...），把該網域補進對應 directive。
#
# 來源清單依據：GSI 登入 accounts.google.com（script/frame/connect）、Supabase REST/auth
# *.supabase.co（connect）、分享圖卡 canvas blob:（img）、Google 頭像 *.googleusercontent.com（img）、
# Play Billing 付款方式 play.google.com（connect——PaymentRequest 用網址當付款識別碼時，
# 規範要求該網址 origin 必須在 connect-src 內，否則建構直接拋 RangeError violates CSP，
# 這正是 vc14 鑽石購買點下去無反應的真因）。
# http://127.0.0.1:47591 = AdMob 獎勵廣告本機橋接（android/…/AdBridgeService.kt），
# loopback 對 HTTPS 頁面不算 mixed content，但仍需在 connect-src 白名單內 fetch 才不被擋。
# ⚠️ 未來加新外部服務（AdSense pagead2.googlesyndication.com 等）記得同步補進 CSP。
# Permissions-Policy 刻意不禁 payment：TWA 接 Play Billing 的 Digital Goods API 會用到。

/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=(), usb=(), serial=()
  Content-Security-Policy: default-src 'self'; script-src 'self' https://accounts.google.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https://*.googleusercontent.com; connect-src 'self' https://*.supabase.co https://accounts.google.com https://play.google.com http://127.0.0.1:47591; frame-src https://accounts.google.com; worker-src 'self'; manifest-src 'self'; base-uri 'self'; object-src 'none'; form-action 'self'; frame-ancestors 'none'
