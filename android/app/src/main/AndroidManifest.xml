<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- AdMob App ID（跟正式 TWA 版共用同一個 AdMob 帳戶下的 App，見 CLAUDE.md
             廣告雙軌架構段落）。這只是「註冊」用途，實際播放哪支廣告單元由
             src/lib/ads.ts 呼叫 AdMob.prepareRewardVideoAd({adId}) 決定。 -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8981745966447649~3758790919" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Google Play Billing（鑽石購買/永久去廣告 IAP）必要權限。@capgo/native-purchases
         外掛本身的 AndroidManifest 是空的（不像 admob 外掛會自動帶 INTERNET 權限），
         這條要手動補，漏了 Play Billing 會直接連不上。 -->
    <uses-permission android:name="com.android.vending.BILLING" />
    <!-- 2026-07-10 上傳 Play Console 時報錯發現：新版 play-services-ads SDK（20.4.0+）
         不會自動帶這條權限，Play Console 的「廣告 ID 宣告」若勾了「應用程式使用
         廣告 ID」但 manifest 沒這條，廣告 ID 會被清成一串零，影響廣告投放/數據分析/
         收益。@capacitor-community/admob 外掛的 manifest 也沒帶這條（只有
         INTERNET/ACCESS_NETWORK_STATE），要手動補。 -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
</manifest>
